<?php 

// Variabel $_server
// berguna untuk menampilkan informasi tentang server yang digunakan

// echo json_encode($_server)

// variabel $_POST
// passing data dari body request

// Variabel $_SESSION
// variabel yang menyimpan data pengguna, jadi jika pengguna mengunjungi situs tersebut dengan waktu tertentu, data akan kembali tersaji tanpa harus di isi kembali
// session bisa diatus waktunya, secara defaultnya 24 mnt

// Variabel $_COOKIE
// durasinya lebih panjang dari session

// Variabel $_REQUEST
// menyimpan gabungan nilai dari $_GET, $_POST, $_COOKIE yang di kirim bersamaan dengan request user

// Variabel $_FILES
// Biasanya digunakan untuk fitur upload file

// Variabel $_ENV
// Digunakan dalam framework yang berisi database.env

// Variabel $_GLOBALS
// Biar bisa akses ke semua file

?>